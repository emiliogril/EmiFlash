import"./ItemListContainer.css";

const ItemListContainer = (props) =>{
    return (
        <h1 className="titulo">
            {props.titulo}
        </h1> 
    );
};

export default ItemListContainer;
